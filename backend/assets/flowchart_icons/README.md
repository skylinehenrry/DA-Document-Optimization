# Flowchart Icon Assets

Reusable SVG icons for generated workflow flowcharts.

- These icons are source assets for the backend renderer.
- The renderer can inline the SVG markup into standalone HTML output.
- Icons use `currentColor` so CSS can apply each file type's accent color.
- Keep these in `backend/assets` because flowchart generation is backend-owned.
